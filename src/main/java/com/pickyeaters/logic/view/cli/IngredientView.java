package com.pickyeaters.logic.view.cli;

public class IngredientView {
/*
    private IngredientController controller = new IngredientViewController(this);

    public void show(String[] args) {
        controller.request(args);
    }

    public IngredientBean askIngredient() {
        Scanner userInput = new Scanner(System.in);
        IngredientBean ingredientBean = new IngredientBean();
        System.out.print("Ingredient name: ");
        ingredientBean.setName(userInput.nextLine());
        return ingredientBean;
    }

    public void request(String[] args) {
        switch(args[1].toLowerCase()) {
            case "add":
                requestAdd();
                break;
            default:
                throw new UnsupportedOperationException("Cannot execute " + args[1].toLowerCase());
        }
    }

    private void requestAdd() {
        IngredientBean ingredientBean = view.askIngredient();
        controller.add(ingredientBean.getName());
    }
 */
}
